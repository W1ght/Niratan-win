<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="Hoshi.Views.Pages.VideoLibraryPage"
      x:Name="ThisPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converters="using:Hoshi.Helpers.UI.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vmc="using:Hoshi.ViewModels.Components"
      mc:Ignorable="d">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <DataTemplate x:Key="VideoItemTemplate" x:DataType="vmc:VideoItemViewModel">
            <Button Width="240"
                    Padding="0"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    AutomationProperties.AutomationId="{x:Bind AutomationId, Mode=OneTime}"
                    AutomationProperties.Name="{x:Bind Video.Title, Mode=OneTime}"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="VideoButton_Click">
                <StackPanel Spacing="8">
                    <Border Height="138"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            CornerRadius="8">
                        <Grid>
                            <FontIcon FontSize="44"
                                      Glyph="&#xE714;"
                                      Opacity="0.35"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <StackPanel Spacing="4">
                        <TextBlock Text="{x:Bind Video.Title, Mode=OneTime}"
                                   TextWrapping="WrapWholeWords"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" />
                    </StackPanel>
                </StackPanel>
            </Button>
        </DataTemplate>
    </Page.Resources>

    <Grid RowDefinitions="Auto,*">
        <Grid Grid.Row="0"
              Padding="20,14,36,16"
              ColumnDefinitions="Auto,*,Auto"
              ColumnSpacing="12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <FontIcon FontSize="24" Glyph="&#xE714;" />
                <TextBlock x:Uid="VideoLibraryPageTitle"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           Style="{StaticResource TitleTextBlockStyle}"
                           Text="Videos" />
            </StackPanel>

            <Button Grid.Column="2"
                    x:Uid="ImportVideoButton"
                    AutomationProperties.AutomationId="ImportVideoButton"
                    AutomationProperties.Name="Import video"
                    Command="{x:Bind ViewModel.ImportVideoCommand}"
                    ToolTipService.ToolTip="Import video">
                <FontIcon Glyph="&#xE8E5;" />
            </Button>
        </Grid>

        <ProgressRing Grid.Row="1"
                      Width="60"
                      Height="60"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      IsActive="{x:Bind ViewModel.IsContentLoading, Mode=OneWay}"
                      Visibility="{x:Bind ViewModel.IsContentLoading, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" />

        <TextBlock Grid.Row="1"
                   x:Uid="NoVideosText"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Style="{StaticResource BodyLargeStrongTextBlockStyle}"
                   Text="No videos imported."
                   Visibility="{x:Bind ViewModel.NoVideos, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" />

        <GridView Grid.Row="1"
                  AutomationProperties.AutomationId="VideoGridView"
                  Padding="0,0,16,0"
                  IsItemClickEnabled="True"
                  ItemClick="GridView_ItemClick"
                  ItemTemplate="{StaticResource VideoItemTemplate}"
                  ItemsSource="{x:Bind ViewModel.Videos, Mode=OneWay}"
                  SelectionMode="None" />
    </Grid>
</Page>
