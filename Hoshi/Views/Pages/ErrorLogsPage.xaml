<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="Hoshi.Views.Pages.ErrorLogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="using:Hoshi.Models"
      mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">
        <StackPanel Grid.Row="0"
                    MaxWidth="1016"
                    Padding="20,14,36,16"
                    Orientation="Horizontal"
                    Spacing="8">
            <FontIcon FontSize="24" Glyph="&#xE7BA;" />
            <TextBlock VerticalAlignment="Center" Style="{StaticResource TitleTextBlockStyle}" Text="错误日志" />
            <Button Margin="16,0,0,0" Command="{x:Bind ViewModel.RefreshLogsCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    <TextBlock Text="刷新" />
                </StackPanel>
            </Button>
            <TextBlock VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="{x:Bind ViewModel.LogStatusText, Mode=OneWay}" />
        </StackPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{x:Bind ViewModel.ErrorLogs, Mode=OneWay}"
                  Padding="0,0,16,16"
                  AutomationProperties.AutomationId="ErrorLogsList">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LogEntry">
                    <Grid Padding="4,3" ColumnDefinitions="Auto, Auto, Auto, *">
                        <TextBlock Grid.Column="0"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   FontFamily="Consolas"
                                   FontSize="11"
                                   Text="{x:Bind Timestamp}"
                                   Width="140" />
                        <Border Grid.Column="1"
                                CornerRadius="3"
                                Padding="3,1"
                                Margin="6,0"
                                VerticalAlignment="Center">
                            <Border.Background>
                                <SolidColorBrush Color="{x:Bind LevelColor}" />
                            </Border.Background>
                            <TextBlock FontFamily="Consolas"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       Text="{x:Bind Level}" />
                        </Border>
                        <TextBlock Grid.Column="2"
                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                   FontFamily="Consolas"
                                   FontSize="11"
                                   Margin="6,0"
                                   Text="{x:Bind SourceContext}" />
                        <TextBlock Grid.Column="3"
                                   FontFamily="Consolas"
                                   FontSize="11"
                                   Text="{x:Bind Message}"
                                   TextTrimming="CharacterEllipsis" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
