<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="Niratan.Views.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="using:CommunityToolkit.WinUI"
      xmlns:models="using:Niratan.Models"
      xmlns:vmp="using:Niratan.ViewModels.Pages"
      mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">
        <!-- Header -->
        <StackPanel Grid.Row="0"
                    MaxWidth="1016"
                    Padding="20,14,36,16"
                    Orientation="Horizontal"
                    Spacing="8">
            <FontIcon FontSize="24" Glyph="&#xE8F1;" />
            <TextBlock VerticalAlignment="Center" Style="{StaticResource TitleTextBlockStyle}" Text="Logs" />
            <Button Margin="16,0,0,0" Command="{x:Bind ViewModel.RefreshLogsCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    <TextBlock Text="Refresh" />
                </StackPanel>
            </Button>
            <TextBlock VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Text="{x:Bind ViewModel.LogStatusText, Mode=OneWay}" />
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="0,0,16,16" Spacing="16">
                <!-- Error Logs (shown first, more important) -->
                <StackPanel>
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Margin="1,12,0,6">
                        <Span><Bold>Errors &amp; Warnings</Bold></Span>
                    </TextBlock>
                    <ListView ItemsSource="{x:Bind ViewModel.ErrorLogs, Mode=OneWay}"
                              MaxHeight="360"
                              AutomationProperties.AutomationId="ErrorLogsList">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:LogEntry">
                                <Grid Padding="4,3" ColumnDefinitions="Auto, Auto, Auto, *">
                                    <TextBlock Grid.Column="0"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Text="{x:Bind Timestamp}"
                                               Width="140" />
                                    <Border Grid.Column="1"
                                            CornerRadius="3"
                                            Padding="3,1"
                                            Margin="6,0"
                                            VerticalAlignment="Center">
                                        <Border.Background>
                                            <SolidColorBrush Color="{x:Bind LevelColor}" />
                                        </Border.Background>
                                        <TextBlock FontFamily="Consolas"
                                                   FontSize="11"
                                                   FontWeight="Bold"
                                                   Foreground="White"
                                                   Text="{x:Bind Level}" />
                                    </Border>
                                    <TextBlock Grid.Column="2"
                                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Margin="6,0"
                                               Text="{x:Bind SourceContext}" />
                                    <TextBlock Grid.Column="3"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Text="{x:Bind Message}"
                                               TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <!-- All Recent Logs -->
                <StackPanel>
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Margin="1,12,0,6">
                        <Span><Bold>Recent Logs</Bold></Span>
                    </TextBlock>
                    <ListView ItemsSource="{x:Bind ViewModel.RecentLogs, Mode=OneWay}"
                              MaxHeight="480"
                              AutomationProperties.AutomationId="RecentLogsList">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:LogEntry">
                                <Grid Padding="4,3" ColumnDefinitions="Auto, Auto, Auto, *">
                                    <TextBlock Grid.Column="0"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Text="{x:Bind Timestamp}"
                                               Width="140" />
                                    <Border Grid.Column="1"
                                            CornerRadius="3"
                                            Padding="3,1"
                                            Margin="6,0"
                                            VerticalAlignment="Center">
                                        <Border.Background>
                                            <SolidColorBrush Color="{x:Bind LevelColor}" />
                                        </Border.Background>
                                        <TextBlock FontFamily="Consolas"
                                                   FontSize="11"
                                                   FontWeight="Bold"
                                                   Foreground="White"
                                                   Text="{x:Bind Level}" />
                                    </Border>
                                    <TextBlock Grid.Column="2"
                                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Margin="6,0"
                                               Text="{x:Bind SourceContext}" />
                                    <TextBlock Grid.Column="3"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Text="{x:Bind Message}"
                                               TextTrimming="CharacterEllipsis" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
